{
  "name": "@fsdev/b",
  "version": "2.0.2-beta.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:test": "rimraf lib && vite build",
    "build:prod": "rimraf lib && vite build"
  },
  "author": "",
  "license": "ISC",
  "publishConfig": {
    "registry": "http://nexus.fscut.com/repository/npm-dev/"
  }
}